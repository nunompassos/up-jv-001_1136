package tech.ada.exemplos.fluxo;

import java.util.Random;

public class If {
    public static void main(String[] args) {
        boolean condicao = new Random().nextBoolean();
        if (condicao) {
            // Bloco de código a ser executado se a condição for verdadeira
        }
    }
}
